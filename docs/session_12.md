Session 9 (agent log) — mobile navigation parity, db-path URL contract, anchored seed dates (PAD v1.6)

Session 9 began with the standard refresh (fresh clone at `c625944`; the workspace had been reset) and a full re-read of the four core documents (AGENTS, CLAUDE, README, PAD v1.5) plus `docs/session_10.md` and `docs/session_11.md`. The uploaded 709-line operating instruction set was internalized (evidence-based verification, reproduce-before-fix, never weaken a guardrail, surgical diffs, clean handoff). Baseline revalidation from the fresh workspace: lint 0, tsc 0, 85/85 vitest, db push/seed/verify:analytics PASS, dev server healthy.

A live-reference audit (fresh agent-browser login with the operator account, computed-style + DOM extraction at 1360×900 and 390×844) produced the session's findings. Three display-layer red herrings were caught by the evidence discipline before they could cause damage: `sign-in-dialog.tsx` line 20 renders as `const ode, setMode]` through every text display layer, but the on-disk bytes are `const [mode, setMode]` (verified via char codes 91,109, the TS parser's zero diagnostics, `bun build` compiling, and eslint/tsc exiting 0) — the same artifact mangles `[main]` in `ci.yml` displays. No "fix" was applied to either.

## Remediation (executed this session)

| # | Gap (source: live-reference audit + codebase verification) | Fix |
|---|---|---|
| P1 | **No mobile navigation.** The rail was `hidden md:flex` — below 768px users could not navigate at all. The reference ships a floating bottom pill below `lg` and a rail at `lg`+ (both `lg`, not `md`), extracted precisely: wrapper `fixed bottom-0 inset-x-0 z-50 justify-center pb-4 pointer-events-none`; frosted 42px container (radius 100px, `rgba(255,255,255,0.1)` + 20px backdrop blur, 4px gap/padding); active = white 48px pill with a 14px glyph + 14px/500 Inter label rendered in place; inactive = 34px `#DFDFDF` circles | New `mobile-nav.tsx`; rail restyled to the reference's true geometry (active = 60×60 white icon square + growing label pill, inactive = 40px pills, 10px glyphs, 14px/400 Inter labels, 16px rail padding, 4px gaps); `nav-items.tsx` extracted as the shared model (items, order, `isNavItemActive`, the reference's own inlined SVG glyphs — verified byte-identical to the live app's assets); 6 unit tests pin the model; 7 E2E specs pin the pill (renders, taps, active states, `lg` swap, no 390px overflow) |
| P2 | **Header mobile deviations.** SupplyChain pill hidden below `sm` (reference: visible), New Product always visible (reference: `hidden md:flex`), Sign In/Out never collapse (reference: 50px `#DFDFDF` circle with a 34px black core below `md`), pill labels rendered in system-ui at 14px (reference: Inter 16px/400, the orange label wrapping to two lines) | Header rebuilt per the audit; `--font-brand` (Inter) utility added to `@theme`; `--font-sans` repaired — it pointed at an undefined `--font-geist-sans`, silently degrading the whole app's body font stack; the reference's User/SignIn/SignOut/Plus glyphs inlined |
| P3 | **KPI grid mobile + value color.** Clone stacked all four tiles 1-column; reference does [Total SKUs \| Inventory Value] as a row, gauges full-width, side-by-side from `sm`; the black tile's numeral must be orange (verified on the live reference), and the desktop grid's hidden overflow (`scrollWidth` 573 > 390) came from grid items without `min-w-0` | `page.tsx` restructured (outer `flex-col lg:grid-cols-[331fr_1fr]`, section A `flex-row lg:flex-col`, section B `flex-col sm:flex-row`); KpiCard black variant numeral → `text-primary`; `min-w-0` on the chart sections; zero horizontal overflow on every route at 390px (E2E-pinned) |
| P4 | **`DATABASE_URL` contract unimplemented.** `.env.example` referenced `src/lib/db-path.ts` since v1.2 — it never existed; empirically, the Prisma CLI resolves env-provided relative `file:` URLs against the `.env` location, so `file:../db/custom.db` from the repo root landed one level ABOVE the repo (reproduced three ways) | TDD: 12-test `src/lib/db-path.test.ts` written red first, then `src/lib/db-path.ts` (anchors relative URLs at the `prisma/` schema dir via module-location walk-up with CWD fallback, memoized, never throws; absolute/PG URLs pass through); wired into `src/lib/db.ts`, `prisma/seed.ts`, `scripts/verify-analytics.ts`; new `scripts/db-cli.ts` wrapper routes `db:push`/`db:migrate`/`db:reset` so the CLI sees the same absolute URL; `.env` keeps the required `DATABASE_URL="file:../db/custom.db"` and the db file lands at `<repo>/db/custom.db` — verified end-to-end (push → seed → verify → dev server health, zero strays) |
| P5 | **E2E time bomb: date + day-bucket drift.** The specs assert the reference's fixed dates (07.06.26, 07.12.26) and badges (+2/−1/0/+1/+2), but the seed computed PO dates relative to seed-day and the movers windows are day-bucketed 30d — surfaced when the session crossed UTC midnight between seed and E2E (two tests failed with −1/−2 badges and shifted dates; a fresh same-day reseed reproduced green, proving the mechanism) | PO dates + market-trends quarter pinned to `REFERENCE_ANCHOR` (2026-09-20, the capture day) exactly like the reference's static data; the ledger stays `now`-relative (velocity needs live windows); `db:seed` now detects a stale seeded ledger (newest sale not today) and rebuilds it, making the documented `db:seed && verify:analytics` recovery self-healing |
| P6 | **Weakened build gate:** `next.config.ts` shipped `typescript.ignoreBuildErrors: true` (tsc passes clean — the suppression was dead weight and a guardrail violation) | Removed; `bun run build` green with types enforced; `devIndicators: false` added (the dev-tools badge overlapped the mobile nav in captures — an overlay `<nextjs-portal>` element hidden at capture time for the screenshot set) |
| P7 | **Docs drift:** README/AGENTS/CLAUDE/PAD claimed schema-relative Prisma CLI resolution (factually wrong for env URLs), stale counts (85/31), `.env.example` referenced a nonexistent `docs/DEPLOYMENT.md §4`, and `env.ts`'s error message showed the wrong example URL | All aligned to PAD v1.6; `.env.example` rewritten to describe the actual db-path + db-cli mechanism (with the exported-var precedence note); env.ts message fixed |

Also this session: 18 refreshed/new dev-server captures in `docs/screenshots/` (11 desktop + 7 mobile at 390×844), visual parity re-verified against fresh reference captures via computed-style comparison (pill 42px/100px-radius/blur-20px, active 48px white, circles 34px `#DFDFDF`, rail 208px items at x=16, black-tile numeral orange, IM label two lines) — the DOM measurements match the reference on every audited value.

## Session 9 Outcome

- All gates green at delivery: lint 0 · tsc 0 · **103/103 unit** · verify:analytics PASS ($202,610 basis; velocities 1.5/1.2/0.8/0.7/0.4/0.0; badges +2/−1/0/+1/+2) · clean production build with types enforced · **38/38 chromium E2E** (31 existing + 7 mobile)
- Mobile navigation verified with agent-browser at 390×844 against the live reference (login round-trip + per-route active states + zero horizontal overflow); screenshots under `docs/screenshots/`
- Docs at PAD v1.6; `.env.example` accurate; db file contract proven at `<repo>/db/custom.db`
