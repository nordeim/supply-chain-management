# Dependencies
node_modules/

# Next.js build output
.next/
out/
next-env.d.ts

# Logs
dev.log
server.log
*.log

# Test artifacts (regenerated by `bun run test:e2e`)
playwright-report/
test-results/

# Automation-sandbox scaffolding — this workspace's harness occasionally
# auto-commits untracked files; these never belong in the repository
# (auth.json especially: it stores agent-browser credentials).
.zscripts/
agent-browser.json
auth.json
Caddyfile
download/
examples/
mini-services/
tool-results/
upload/
worklog.md
tests/

# Build artifacts
tsconfig.tsbuildinfo

# Environment & secrets — never commit
.env
.env.local
.env.*.local
*.key
ssh-key.txt

# Databases (regenerate with db:push + db:seed)
db/*.db
db/*.db-journal

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
